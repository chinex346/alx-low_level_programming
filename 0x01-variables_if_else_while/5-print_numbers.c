#include <stdio.h>
/**
 * a program that prints all single digit numbers of base 10 starting from 0,
 *  followed by a new line.
 *  Return: 0 Always.
 */
int main(void)
{
	int c;

	for (c = 0; c <10; c++)

	printf("%d\n", c);
	return (0);
}
